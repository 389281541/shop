server:
  port: 8088

spring:
  application:
    name: ${spring.profiles.active}-shop-admin

  profiles:
    active: ${spring.profiles.active}

  cloud:
    config:
      label: master
      uri: http://localhost:4001/master/application-local.yml
      profile: local
#      fail-fast: true
#      name: ${spring.application.name}
#      profile: ${spring.profiles.active}
#      discovery:
#        enabled: true
#        service-id: shop-config
#      label: master

eureka:
  instance:
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://rainbow:112233@${spring.eureka-url}/eureka/

swagger:
  base-package: com.rainbow.admin.controller
  base-path: /**


#开发环境配置
---
spring:
  profiles: local
  eureka-url: localhost:1025

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.rainbow.admin.entity